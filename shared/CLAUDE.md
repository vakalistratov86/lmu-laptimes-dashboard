# CLAUDE.md — shared

## Контекст

Общие типы и модели данных, используемые и в client, и в server (Session, Lap, DriverMetric).

## Правила

- Любое изменение типа здесь — потенциально breaking change для client и server, проверять оба места использования
- Типы телеметрии описывать явными интерфейсами, без `any`
- Не добавлять сюда логику (только типы/константы), бизнес-логика — в server/ или client/lib
